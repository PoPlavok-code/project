from unittest import result


def is_even(x):
    return x % 2 == 0
#проверка что  число четно

result_filter = list(filter(is_even, range(20)))
#проверка последовательности и фильтровка четных чисел от 0 до 19

result_duble = list(map(lambda x: x % 2, range(20)))
#Если вы хотели проверить чётность (остаток):
дубликаты списков
def dup(x):
    return [x,x]
#возвращение списка из двух знач

result_map = list(map(dup,result_filter))
#возвращение списка из двух знач

from itertools import chain
result_chain = list(chain(*result_map))
#распаковка списка и обеденение

numbers = range(1, 101)
от 1 до 100 print   через list
result = [x for x in numbers if x % 3 == 0 or x % 5 == 0]
от 1 до 100 все что делдится на 3 или 5



def print6(xs):
    for i, x in enumerate(xs):
        print(x)
        if i == 5:
            break


i = (x * x for x in range(10))


print(i)
print6("#" * 5)  когда срабатывает остановка break пишешь и продолжает

result = {x = x for x in range (1,100) if x %2 == 0)
отфильтровали по четности и возвели в квадрат


